export type FormData = {
    name: string;
    course: string;
    state: string;
    city: string;
    cities: string[];
  };
  
  export type CityOptions = {
    [key: string]: string[];
  }
  